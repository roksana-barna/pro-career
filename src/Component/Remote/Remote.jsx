import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Remote = () => {
    const remote =useLoaderData()
    console.log(remote)
    return (
        <div>

            
        </div>
    );
};

export default Remote;